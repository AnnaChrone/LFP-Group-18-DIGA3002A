using UnityEngine;


//Here, when u walk to the stove, for milestone 1, a little UI will pop up of a chef with diff sushis, aswell as your order slip
//You then click 1 of the buttons, and you will be given the sushi of your selection.
//Then, you run the sushi to the delivery station, where if the sushi is right, youll get money, and if wrong, lose money and those ingredients
//We need ingredients reflected in the restaurant too
public class Cooking : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
